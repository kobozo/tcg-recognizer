mlflow-skinny==2.22.0
numpy
pillow
opencv-python-headless
psycopg2-binary
requests==2.33.0
pyyaml==6.0.2
onnxruntime
# DVC core only (sub-project 4): data/pipeline versioning with a local-filesystem
# remote. No cloud extras (s3/gs/azure) — keeps the image lean. Runs inside the
# trainer container via scripts/dvc.sh (this host has no usable pip / host dvc).
dvc==3.*
